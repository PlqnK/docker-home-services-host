---
- name: 'Reboot host'
  ansible.builtin.reboot:

- name: 'Restart autofs service'
  ansible.builtin.systemd:
    name: 'autofs.service'
    state: 'restarted'

- name: 'Reload systemd daemon'
  ansible.builtin.systemd:
    daemon_reload: true
