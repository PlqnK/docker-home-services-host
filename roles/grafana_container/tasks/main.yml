---
- name: 'Configure and manage Grafana container'
  when: (containers_list is not defined or (containers_list is defined and 'grafana' in containers_list))
  block:
    - name: 'Create'
      ansible.builtin.import_tasks: 'create.yml'

    - name: 'Lifecycle'
      ansible.builtin.import_tasks: 'lifecycle.yml'
