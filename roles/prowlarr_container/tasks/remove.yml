---
- name: 'Ensure the user really wants to remove the Prowlarr container'
  ansible.builtin.pause:
    prompt: "WARNING! DO YOU REALLY WANT TO REMOVE THE PROWLARR CONTAINER?\nTHIS ACTION WILL DELETE ALL PROWLARR APP DATA AND IS IRREVERSIBLE!\nAnswer yes in capital letters to confirm"
    echo: true
  register: remove_prompt

- name: "Stop execution if user didn't answer 'YES'"
  ansible.builtin.fail:
    msg: "Stopping playbook execution because the playbook was called with the tag 'containers-remove' but the user didn't want to remove the containers."
  when: (remove_prompt.user_input != "YES")

- name: 'Disable and stop Prowlarr container'
  ansible.builtin.systemd:
    name: 'container-prowlarr.service'
    daemon_reload: true
    enabled: false
    state: 'stopped'
  register: systemd_unit
  failed_when: (systemd_unit.failed and not "Could not find the requested service" in systemd_unit.msg)

- name: 'Remove Prowlarr container systemd unit'
  ansible.builtin.file:
    path: '/etc/systemd/system/container-prowlarr.service'
    state: 'absent'
  notify: 'Reload systemd daemon'

- name: 'Remove Prowlarr data bind mount path'
  ansible.builtin.file:
    path: '{{ containers_storage_path }}/data/prowlarr'
    state: 'absent'
