---
- name: 'Pull changedetection image'
  containers.podman.podman_image:
    name: '{{ containers_images.changedetection }}:{{ containers_images_tags.changedetection }}'
    pull: true
    force: true
    state: 'present'
  register: 'container_image_pull'
  tags: ['never', 'containers-pull', 'containers-update']

- name: 'Update changedetection tag in quadlet unit' # noqa: no-handler
  ansible.builtin.template:
    src: 'changedetection.container.j2'
    dest: '/etc/containers/systemd/changedetection.container'
    owner: 'root'
    group: 'root'
    mode: '0644'
  when: (container_image_pull.changed)
  register: 'quadlet_unit'
  tags: ['never', 'containers-update']

- name: 'Restart changedetection container'
  ansible.builtin.systemd_service:
    name: 'changedetection.service'
    daemon_reload: '{{ ("true" | bool) if quadlet_unit.changed else ("false" | bool) }}'
    state: 'restarted'
  when: ("containers-restart" in ansible_run_tags or
        ("containers-update" in ansible_run_tags and container_image_pull.changed))
  tags: ['never', 'containers-restart', 'containers-update']

- name: 'Disable changedetection container'
  ansible.builtin.systemd_service:
    name: 'changedetection.service'
    enabled: false
  tags: ['never', 'containers-disable']

- name: 'Stop changedetection container'
  ansible.builtin.systemd_service:
    name: 'changedetection.service'
    state: 'stopped'
  tags: ['never', 'containers-stop']

- name: 'Enable changedetection container'
  ansible.builtin.systemd_service:
    name: 'changedetection.service'
    enabled: true
  tags: ['never', 'containers-enable']

- name: 'Start changedetection container'
  ansible.builtin.systemd_service:
    name: 'changedetection.service'
    state: 'started'
  tags: ['never', 'containers-start']
