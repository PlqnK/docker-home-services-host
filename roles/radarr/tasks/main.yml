---
- name: Create config directory
  file:
    path: "{{ local_container_storage_path }}/radarr/config"
    state: directory
    owner: "{{ docker_runtime_user_name }}"
    group: "{{ docker_runtime_group_name }}"
    mode: 0755

- name: Create the Radarr container
  docker_container:
    name: radarr
    image: linuxserver/radarr
    restart_policy: unless-stopped
    networks:
      - name: proxy
    volumes:
      - "{{ local_container_storage_path }}/radarr/config:/config"
      - "{{ nfs_mount_path }}/downloads:/downloads:slave"
      - "{{ nfs_mount_path }}/medias/movies:/movies:slave"
    env:
      TZ: "{{ timezone }}"
      PUID: "{{ docker_runtime_user_id }}"
      PGID: "{{ docker_runtime_group_id }}"
    labels:
      traefik.enable: "true"
      traefik.docker.network: "proxy"
      traefik.port: "7878"
      traefik.frontend.rule: "Host:radarr.{{ domain_name }}"
    pull: yes
    state: started
