---
- name: 'Configure and manage syslog-ng container'
  when: (containers_list is not defined or (containers_list is defined and 'syslog-ng' in containers_list))
  block:
    - name: 'Create'
      ansible.builtin.include_tasks: 'create.yml'

    - name: 'Lifecycle'
      ansible.builtin.import_tasks: 'lifecycle.yml'

    - name: 'Remove'
      ansible.builtin.include_tasks: 'remove.yml'
      tags: ['never', 'containers-remove']
