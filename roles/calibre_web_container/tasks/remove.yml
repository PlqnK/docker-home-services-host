---
- name: 'Ensure the user really wants to remove the Calibre-Web container'
  ansible.builtin.pause:
    prompt: "WARNING! DO YOU REALLY WANT TO REMOVE THE CALIBRE-WEB CONTAINER?\nTHIS ACTION WILL DELETE ALL CALIBRE-WEB APP DATA AND IS IRREVERSIBLE!\nAnswer yes in capital letters to confirm"
    echo: true
  register: remove_prompt

- name: "Stop execution if user didn't answer 'YES'"
  ansible.builtin.fail:
    msg: "Stopping playbook execution because the playbook was called with the tag 'containers-remove' but the user didn't want to remove the containers."
  when: (remove_prompt.user_input != "YES")

- name: 'Disable and stop Calibre-Web container'
  ansible.builtin.systemd:
    name: 'container-calibre-web.service'
    daemon_reload: true
    enabled: false
    state: 'stopped'
  register: systemd_unit
  failed_when: (systemd_unit.failed and not "Could not find the requested service" in systemd_unit.msg)

- name: 'Remove Calibre-Web container systemd unit'
  ansible.builtin.file:
    path: '/etc/systemd/system/container-calibre-web.service'
    state: 'absent'
  notify: 'Reload systemd daemon'

- name: 'Remove Calibre-Web data bind mount path'
  ansible.builtin.file:
    path: '{{ containers_storage_path }}/data/calibre-web'
    state: 'absent'

- name: 'Check if provisioning a Vagrant VM'
  ansible.builtin.getent:
    database: 'passwd'
    key: 'vagrant'
    fail_key: false

- name: 'Remove Calibre-Web CNAME record'
  community.general.gandi_livedns:
    api_key: '{{ gandi_api_key }}'
    domain: '{{ domain_name }}'
    type: 'CNAME'
    record: 'calibre-web'
    values:
      - '{{ ansible_hostname }}'
    state: 'absent'
  when: (getent_passwd.vagrant is none)
  delegate_to: 'localhost'
