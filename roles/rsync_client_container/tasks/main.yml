---
- name: 'Configure and manage autorestic container'
  when: (containers_list is not defined or (containers_list is defined and 'rsync-client' in containers_list))
  block:
    - name: 'Create'
      ansible.builtin.import_tasks: 'create.yml'

    - name: 'Lifecycle'
      ansible.builtin.import_tasks: 'lifecycle.yml'
