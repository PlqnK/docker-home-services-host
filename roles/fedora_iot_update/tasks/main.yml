---
- name: 'Update and reboot'
  tags: ['never', 'update-os']
  block:
    - name: 'Update OS'
      ansible.posix.rpm_ostree_upgrade:
      notify: 'Reboot host'

    - name: 'Reboot host if OS has been upgraded'
      ansible.builtin.meta: 'flush_handlers'
