---
- name: 'Pull Muxarr image'
  containers.podman.podman_image:
    name: '{{ containers_images.muxarr }}:{{ containers_images_tags.muxarr }}'
    pull: true
    force: true
    state: 'present'
  register: 'container_image_pull'
  tags: ['never', 'containers-pull', 'containers-update']

- name: 'Update Muxarr tag in quadlet unit' # noqa: no-handler
  ansible.builtin.template:
    src: 'muxarr.container.j2'
    dest: '/etc/containers/systemd/muxarr.container'
    owner: 'root'
    group: 'root'
    mode: '0644'
  when: (container_image_pull.changed)
  register: 'quadlet_unit'
  tags: ['never', 'containers-update']

- name: 'Restart Muxarr container'
  ansible.builtin.systemd_service:
    name: 'muxarr.service'
    daemon_reload: '{{ ("true" | bool) if quadlet_unit.changed else ("false" | bool) }}'
    state: 'restarted'
  when: ("containers-restart" in ansible_run_tags or
        ("containers-update" in ansible_run_tags and container_image_pull.changed))
  tags: ['never', 'containers-restart', 'containers-update']

- name: 'Disable Muxarr container'
  ansible.builtin.systemd_service:
    name: 'muxarr.service'
    enabled: false
  tags: ['never', 'containers-disable']

- name: 'Stop Muxarr container'
  ansible.builtin.systemd_service:
    name: 'muxarr.service'
    state: 'stopped'
  tags: ['never', 'containers-stop']

- name: 'Enable Muxarr container'
  ansible.builtin.systemd_service:
    name: 'muxarr.service'
    enabled: true
  tags: ['never', 'containers-enable']

- name: 'Start Muxarr container'
  ansible.builtin.systemd_service:
    name: 'muxarr.service'
    state: 'started'
  tags: ['never', 'containers-start']
