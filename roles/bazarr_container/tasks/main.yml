---
- name: 'Configure and manage Bazarr container'
  when: (containers_list is not defined or (containers_list is defined and 'bazarr' in containers_list))
  block:
    - name: 'Create'
      ansible.builtin.import_tasks: 'create.yml'

    - name: 'Lifecycle'
      ansible.builtin.import_tasks: 'lifecycle.yml'
