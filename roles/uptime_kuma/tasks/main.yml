---
- name: 'Configure and manage Uptime Kuma container'
  when: (containers_list is not defined or (containers_list is defined and 'uptime-kuma' in containers_list))
  block:
    - name: 'Create'
      ansible.builtin.include_tasks: 'create.yml'

    - name: 'Lifecycle'
      ansible.builtin.import_tasks: 'lifecycle.yml'

    - name: 'Remove'
      ansible.builtin.import_tasks: 'remove.yml'
      tags: ['never', 'containers-remove']
