USER="myuser"
STORAGE_SERVER_NAME="myserver"
AUTO_MASTER="/-	/etc/auto.${STORAGE_SERVER_NAME} --ghost,--timeout=60"
LOCAL_STORAGE="/opt/containers"
LOCAL_STORAGE_DIRS="${LOCAL_STORAGE}/{bazarr,calibre-web,collaboraonline,jackett,komga,lazylibrarian,lidarr,mylar,nzbhydra,ombi,openvpn-client,overseerr,radarr,sabnzbd,sonarr,tautulli,traefik,transmission}/config ${LOCAL_STORAGE}/{nextcloud_mariadb,plexmediaserver,ttrss_postgres}/db ${LOCAL_STORAGE}/{nextcloud_mariadb,ttrss_postgres}/dumps ${LOCAL_STORAGE}/nextcloud"
