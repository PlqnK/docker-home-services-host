NETWORK_TRAEFIK_IPV6_PREFIX="fd00:dead:beef:1::/64"
NETWORK_PLEX_IPV6_PREFIX="fd00:dead:beef:2::/64"
NETWORK_WEB_IPV6_PREFIX="fd00:dead:beef:3::/64"
NETWORK_VPN_IPV6_PREFIX="fd00:dead:beef:4::/64"
USER="myuser"
STORAGE_SERVER_NAME="myserver"
AUTO_MASTER="/-	/etc/auto.${STORAGE_SERVER_NAME} --ghost,--timeout=60"
LOCAL_STORAGE="/opt/containers"
LOCAL_STORAGE_DIRS="${LOCAL_STORAGE}/{bazarr,calibre,calibre-web,collaboraonline,komga,lidarr,mylar,openvpn-client,overseerr,prowlarr,radarr,sabnzbd,sonarr,tautulli,traefik,transmission}/config ${LOCAL_STORAGE}/{nextcloud_mariadb,plexmediaserver,ttrss_postgres}/db ${LOCAL_STORAGE}/{nextcloud_mariadb,ttrss_postgres}/dumps ${LOCAL_STORAGE}/nextcloud"
